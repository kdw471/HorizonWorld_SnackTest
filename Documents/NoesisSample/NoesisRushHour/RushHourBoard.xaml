<!--
  NoesisRushHour - the puzzle board screen, drawn by Noesis.  GENERATED: edit
  Documents/Tools/build_noesis_rushhour_xaml.js and run it again, do not edit this file.

  Rows follow PuzzleBoardUI_Panel: top inset 6% / board 7 : aux 3 / bottom inset 8%.
  The bars are laid out here. The board, the end points and the pieces are positioned by the
  script (NoesisRushHour_Panel.ts) in root pixels, because MouseDragElementBehavior reports X / Y
  in root pixels. The script learns the root size from OnRootWidth / OnRootHeight below.
-->
<Grid
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
	x:Name="Root" Background="#FF14171F">

	<Grid.Resources>
		<Style x:Key="BarButton" TargetType="Button">
			<Setter Property="Foreground" Value="#FFFFFFFF"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="CellBox" TargetType="Border">
			<Setter Property="Margin" Value="1.5"/>
			<Setter Property="CornerRadius" Value="6"/>
			<Setter Property="Background" Value="#FF1F212E"/>
		</Style>

		<Style x:Key="PortBase" TargetType="Border">
			<Setter Property="Margin" Value="1.5"/>
			<Setter Property="CornerRadius" Value="6"/>
			<Setter Property="Background" Value="#FF0B0D12"/>
		</Style>
		<Style x:Key="PortRed" TargetType="Border" BasedOn="{StaticResource PortBase}">
			<Setter Property="Background" Value="#40E11D48"/>
			<Setter Property="BorderBrush" Value="#FFE11D48"/>
			<Setter Property="BorderThickness" Value="4"/>
		</Style>
		<Style x:Key="PortBlue" TargetType="Border" BasedOn="{StaticResource PortBase}">
			<Setter Property="Background" Value="#403B82F6"/>
			<Setter Property="BorderBrush" Value="#FF3B82F6"/>
			<Setter Property="BorderThickness" Value="4"/>
		</Style>

		<Style x:Key="FaceBlock" TargetType="Border">
			<Setter Property="CornerRadius" Value="10"/>
			<Setter Property="Background" Value="#FF64748B"/>
			<Setter Property="BorderBrush" Value="#FF94A3B8"/>
			<Setter Property="BorderThickness" Value="3"/>
			<Setter Property="IsHitTestVisible" Value="False"/>
		</Style>
		<Style x:Key="FaceRed" TargetType="Border" BasedOn="{StaticResource FaceBlock}">
			<Setter Property="Background" Value="#FFE11D48"/>
			<Setter Property="BorderBrush" Value="#FFFDA4AF"/>
		</Style>
		<Style x:Key="FaceBlue" TargetType="Border" BasedOn="{StaticResource FaceBlock}">
			<Setter Property="Background" Value="#FF3B82F6"/>
			<Setter Property="BorderBrush" Value="#FF93C5FD"/>
		</Style>
		<Style x:Key="PieceLabel" TargetType="TextBlock">
			<Setter Property="Foreground" Value="#FFFFFFFF"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="IsHitTestVisible" Value="False"/>
		</Style>
	</Grid.Resources>

	<!-- Root size report. The press trigger doubles as "tap to start", so the size is known before play. -->
	<b:Interaction.Triggers>
		<b:EventTrigger EventName="Loaded">
			<b:InvokeCommandAction Command="{Binding OnRootWidth}" CommandParameter="{Binding ActualWidth, ElementName=Root}"/>
			<b:InvokeCommandAction Command="{Binding OnRootHeight}" CommandParameter="{Binding ActualHeight, ElementName=Root}"/>
		</b:EventTrigger>
		<b:EventTrigger EventName="SizeChanged">
			<b:InvokeCommandAction Command="{Binding OnRootWidth}" CommandParameter="{Binding ActualWidth, ElementName=Root}"/>
			<b:InvokeCommandAction Command="{Binding OnRootHeight}" CommandParameter="{Binding ActualHeight, ElementName=Root}"/>
		</b:EventTrigger>
		<b:EventTrigger EventName="PreviewMouseLeftButtonDown">
			<b:InvokeCommandAction Command="{Binding OnRootWidth}" CommandParameter="{Binding ActualWidth, ElementName=Root}"/>
			<b:InvokeCommandAction Command="{Binding OnRootHeight}" CommandParameter="{Binding ActualHeight, ElementName=Root}"/>
			<b:InvokeCommandAction Command="{Binding OnRootTap}"/>
		</b:EventTrigger>
	</b:Interaction.Triggers>

	<Grid.RowDefinitions>
		<RowDefinition Height="6*"/>
		<RowDefinition Height="60.2*"/>
		<RowDefinition Height="25.8*"/>
		<RowDefinition Height="8*"/>
	</Grid.RowDefinitions>

	<!-- Top inset: title and move count. -->
	<Grid Grid.Row="0">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="3*"/>
			<ColumnDefinition Width="94*"/>
			<ColumnDefinition Width="3*"/>
		</Grid.ColumnDefinitions>
		<TextBlock Grid.Column="1" Text="{Binding TitleText}" FontSize="{Binding FontLarge}" FontWeight="Bold"
				   Foreground="#FFFFFFFF" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
		<TextBlock Grid.Column="1" Text="{Binding MovesText}" FontSize="{Binding FontLarge}" FontWeight="Bold"
				   Foreground="#FFF2CC40" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
	</Grid>

	<!-- Aux area: hint and Reset. -->
	<Grid Grid.Row="2">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="3*"/>
			<ColumnDefinition Width="94*"/>
			<ColumnDefinition Width="3*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="8*"/>
			<RowDefinition Height="84*"/>
			<RowDefinition Height="8*"/>
		</Grid.RowDefinitions>
		<Border Grid.Row="1" Grid.Column="1" Background="#FF1C1F2B" CornerRadius="14">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="4*"/>
					<ColumnDefinition Width="70*"/>
					<ColumnDefinition Width="2*"/>
					<ColumnDefinition Width="20*"/>
					<ColumnDefinition Width="4*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="15*"/>
					<RowDefinition Height="70*"/>
					<RowDefinition Height="15*"/>
				</Grid.RowDefinitions>
				<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HintText}" FontSize="{Binding FontMedium}"
						   Foreground="#FFCBD5E1" TextWrapping="Wrap" VerticalAlignment="Center"/>
				<Button Grid.Row="1" Grid.Column="3" Style="{StaticResource BarButton}" Background="#FF52576B"
						Content="Reset" FontSize="{Binding FontMedium}" Command="{Binding OnReset}"/>
			</Grid>
		</Border>
	</Grid>

	<!-- Bottom inset: diagnostics (test build only) and Menu. -->
	<Grid Grid.Row="3">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="3*"/>
			<ColumnDefinition Width="72*"/>
			<ColumnDefinition Width="22*"/>
			<ColumnDefinition Width="3*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="14*"/>
			<RowDefinition Height="72*"/>
			<RowDefinition Height="14*"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DiagText}" FontSize="{Binding FontSmall}"
				   Foreground="#FF64748B" TextWrapping="Wrap" VerticalAlignment="Center"/>
		<Button Grid.Row="1" Grid.Column="2" Style="{StaticResource BarButton}" Background="#FF3D455C" Opacity="0.92"
				Content="Menu" FontSize="{Binding FontMedium}" Command="{Binding OnMenu}"/>
	</Grid>

	<!-- Board layer: everything here is placed by the script in root pixels. -->
	<Canvas Grid.RowSpan="4">
		<Border Canvas.Left="{Binding BoardX}" Canvas.Top="{Binding BoardY}" Width="{Binding BoardSize}" Height="{Binding BoardSize}"
				Background="#FF0E1017" CornerRadius="10" IsHitTestVisible="False">
			<UniformGrid Rows="7" Columns="7">
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
				<Border Style="{StaticResource CellBox}"/>
			</UniformGrid>
		</Border>

		<Grid Canvas.Left="{Binding E1X}" Canvas.Top="{Binding E1Y}" Width="{Binding E1Size}" Height="{Binding E1Size}" IsHitTestVisible="False">
			<Border Style="{StaticResource PortBase}"/>
			<Border Style="{StaticResource PortRed}" Opacity="{Binding E1Red}"/>
			<Border Style="{StaticResource PortBlue}" Opacity="{Binding E1Blue}"/>
		</Grid>
		<Grid Canvas.Left="{Binding E2X}" Canvas.Top="{Binding E2Y}" Width="{Binding E2Size}" Height="{Binding E2Size}" IsHitTestVisible="False">
			<Border Style="{StaticResource PortBase}"/>
			<Border Style="{StaticResource PortRed}" Opacity="{Binding E2Red}"/>
			<Border Style="{StaticResource PortBlue}" Opacity="{Binding E2Blue}"/>
		</Grid>

		<Canvas Canvas.Left="{Binding P1TL}" Canvas.Top="{Binding P1TT}" Width="{Binding P1TW}" Height="{Binding P1TH}">
			<Grid Width="{Binding P1W}" Height="{Binding P1H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P1X, Mode=TwoWay}" Y="{Binding P1Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP1Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP1Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP1Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P1Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P1Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P1Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P2TL}" Canvas.Top="{Binding P2TT}" Width="{Binding P2TW}" Height="{Binding P2TH}">
			<Grid Width="{Binding P2W}" Height="{Binding P2H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P2X, Mode=TwoWay}" Y="{Binding P2Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP2Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP2Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP2Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P2Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P2Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P2Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P3TL}" Canvas.Top="{Binding P3TT}" Width="{Binding P3TW}" Height="{Binding P3TH}">
			<Grid Width="{Binding P3W}" Height="{Binding P3H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P3X, Mode=TwoWay}" Y="{Binding P3Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP3Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP3Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP3Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P3Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P3Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P3Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P4TL}" Canvas.Top="{Binding P4TT}" Width="{Binding P4TW}" Height="{Binding P4TH}">
			<Grid Width="{Binding P4W}" Height="{Binding P4H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P4X, Mode=TwoWay}" Y="{Binding P4Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP4Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP4Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP4Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P4Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P4Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P4Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P5TL}" Canvas.Top="{Binding P5TT}" Width="{Binding P5TW}" Height="{Binding P5TH}">
			<Grid Width="{Binding P5W}" Height="{Binding P5H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P5X, Mode=TwoWay}" Y="{Binding P5Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP5Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP5Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP5Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P5Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P5Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P5Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P6TL}" Canvas.Top="{Binding P6TT}" Width="{Binding P6TW}" Height="{Binding P6TH}">
			<Grid Width="{Binding P6W}" Height="{Binding P6H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P6X, Mode=TwoWay}" Y="{Binding P6Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP6Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP6Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP6Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P6Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P6Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P6Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P7TL}" Canvas.Top="{Binding P7TT}" Width="{Binding P7TW}" Height="{Binding P7TH}">
			<Grid Width="{Binding P7W}" Height="{Binding P7H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P7X, Mode=TwoWay}" Y="{Binding P7Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP7Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP7Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP7Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P7Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P7Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P7Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P8TL}" Canvas.Top="{Binding P8TT}" Width="{Binding P8TW}" Height="{Binding P8TH}">
			<Grid Width="{Binding P8W}" Height="{Binding P8H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P8X, Mode=TwoWay}" Y="{Binding P8Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP8Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP8Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP8Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P8Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P8Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P8Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P9TL}" Canvas.Top="{Binding P9TT}" Width="{Binding P9TW}" Height="{Binding P9TH}">
			<Grid Width="{Binding P9W}" Height="{Binding P9H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P9X, Mode=TwoWay}" Y="{Binding P9Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP9Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP9Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP9Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P9Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P9Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P9Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P10TL}" Canvas.Top="{Binding P10TT}" Width="{Binding P10TW}" Height="{Binding P10TH}">
			<Grid Width="{Binding P10W}" Height="{Binding P10H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P10X, Mode=TwoWay}" Y="{Binding P10Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP10Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP10Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP10Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P10Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P10Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P10Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P11TL}" Canvas.Top="{Binding P11TT}" Width="{Binding P11TW}" Height="{Binding P11TH}">
			<Grid Width="{Binding P11W}" Height="{Binding P11H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P11X, Mode=TwoWay}" Y="{Binding P11Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP11Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP11Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP11Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P11Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P11Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P11Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>
		<Canvas Canvas.Left="{Binding P12TL}" Canvas.Top="{Binding P12TT}" Width="{Binding P12TW}" Height="{Binding P12TH}">
			<Grid Width="{Binding P12W}" Height="{Binding P12H}" Background="Transparent">
				<b:Interaction.Behaviors>
					<b:MouseDragElementBehavior ConstrainToParentBounds="True" X="{Binding P12X, Mode=TwoWay}" Y="{Binding P12Y, Mode=TwoWay}"/>
				</b:Interaction.Behaviors>
				<b:Interaction.Triggers>
					<b:EventTrigger EventName="MouseLeftButtonUp">
						<b:InvokeCommandAction Command="{Binding OnP12Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="TouchUp">
						<b:InvokeCommandAction Command="{Binding OnP12Up}"/>
					</b:EventTrigger>
					<b:EventTrigger EventName="LostMouseCapture">
						<b:InvokeCommandAction Command="{Binding OnP12Up}"/>
					</b:EventTrigger>
				</b:Interaction.Triggers>
				<Border Style="{StaticResource FaceBlock}"/>
				<Border Style="{StaticResource FaceRed}" Opacity="{Binding P12Red}"/>
				<Border Style="{StaticResource FaceBlue}" Opacity="{Binding P12Blue}"/>
				<TextBlock Style="{StaticResource PieceLabel}" Text="{Binding P12Label}" FontSize="{Binding FontMedium}"/>
			</Grid>
		</Canvas>

		<!-- Input lock + banner. LockSize is 0 while playing, the board size otherwise. -->
		<Border Canvas.Left="{Binding BoardX}" Canvas.Top="{Binding BoardY}" Width="{Binding LockSize}" Height="{Binding LockSize}"
				Background="#B30A0C12" CornerRadius="10">
			<TextBlock Text="{Binding BannerText}" FontSize="{Binding FontLarge}" FontWeight="Bold" Foreground="#FFF2CC40"
					   HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>
		</Border>
	</Canvas>
</Grid>
